# Gradle Wrapper - Информация

## ✅ Статус: Работает!

Gradle Wrapper настроен и готов к использованию.

## 📁 Структура

```
kent/
├── gradlew              # Скрипт для Linux/Mac
├── gradlew.bat          # Скрипт для Windows
└── gradle/wrapper/
    ├── gradle-wrapper.jar       # JAR файл (63 KB) ✅
    └── gradle-wrapper.properties # Конфигурация
```

## 🚀 Использование

### Windows:
```powershell
.\gradlew.bat build
.\gradlew.bat tasks
```

### Linux/Mac:
```bash
./gradlew build
./gradlew tasks
```

## 📦 Что происходит при первом запуске

1. `gradlew.bat` запускает `gradle-wrapper.jar`
2. Wrapper проверяет наличие Gradle 8.2
3. Если нет - автоматически скачивает из `services.gradle.org`
4. Сохраняет в `~/.gradle/wrapper/dists/`
5. Запускает Gradle с вашей командой

## ⚠️ Важно

- **НЕ нужно** класть `gradle-8.2-bin.zip` в проект
- Wrapper скачает его автоматически при первом запуске
- Дистрибутив сохраняется в пользовательской папке, не в проекте

## 🔧 Добавление в Git (опционально)

Обычно `gradle-wrapper.jar` добавляют в Git для удобства:

```powershell
git add gradle/wrapper/gradle-wrapper.jar
git commit -m "Add gradle-wrapper.jar"
git push
```

Но это не обязательно - GitHub Actions скачает его автоматически.

## ✅ Проверка

После первого запуска проверьте:

```powershell
.\gradlew.bat --version
```

Должно показать: `Gradle 8.2`

